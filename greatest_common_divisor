class Solution(object):
    def findGCD(self, nums):
        l = nums[0]
        s = nums[0]
        for i in nums:
            if i>l:
                l = i
        for i in nums:
            if i<s:
                s = i

        while s:
            l, s = s, l%s
        return l
        
